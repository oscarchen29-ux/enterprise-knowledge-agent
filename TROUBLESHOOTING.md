# 踩過的坑與換模型注意事項

這份文件記錄開發過程中實際遇到的問題、症狀、原因與解法。
**接手部署或修改這個專案之前請先讀過**,大部分的坑都是「不會報錯但結果是錯的」那種,
不知道的話會花很久才找到。

---

## 一、換模型:「更強」不等於「更適合」

**最容易犯的錯,是以為換一個參數量更大、榜單分數更高的模型就會更好。實測會更糟。**

換之前一律先跑:

```bash
python scripts/check_model.py <模型名稱>
```

### 為什麼更大的模型可能跑不動

**1. 塞不進 VRAM 就會 offload 到 CPU**

開發機是 RTX 5070 Laptop,**8,151 MiB**。而 `qwen2.5:14b` 是 **9.0 GB** ——
比整張顯卡還大。Ollama 不會拒絕,它會把裝不下的層丟到 CPU 跑,結果是速度暴跌。

更麻煩的是這件事會**污染你的實驗結論**:本專案早期記錄過「14B 表現不如 7B」,
但那是「完整在 GPU 上的 7B」對「部分在 CPU 上的 14B」,不是純粹的模型能力比較。

**2. 大模型的工具呼叫不一定比較穩**

`TEST_RESULTS.md` 第二輪記錄過:同一道題,**14B 工具呼叫格式錯誤而失敗,7B 反而答對**。
模型把呼叫寫成純文字而不是走 API 的 `tool_calls` 欄位 —— 這跟參數量沒有正相關。

**3. 有些模型根本不支援 tool calling**

這是最致命的,因為**它不會報錯**。Ollama 遇到不支援工具的模型會直接忽略 `tools` 參數,
`agent.py` 每一輪都收到空的 `tool_calls`,判定為「模型認為不需要查文件」,
於是直接拿模型的內建記憶作答 —— 答案看起來很正常,但完全沒有文件依據。

而且 `verify_answer()` 在 `retrieved_context` 為空時會原樣回傳草稿,
**兩道防線同時失效**。

**4. 推理型(thinking / reasoning)模型可能破壞迴圈**

會先輸出一段思考再回答的模型,思考內容可能被當成 `content` 處理,
或干擾工具呼叫的解析。換這類模型務必實測整個迴圈,不能只看單次回答。

### 四個硬性條件

| 條件 | 門檻 | 不符合的後果 |
|---|---|---|
| 支援 tool calling | `capabilities` 含 `tools` | **完全不檢索,靜默失效** |
| context 上限 | ≥ 16384 | 檢索到的文件被截斷 |
| 模型大小 | 小於可用 VRAM | offload 到 CPU,極慢 |
| 中文能力 | 實測 | 簡體有後處理接住,理解力沒有 |

---

## 二、Ollama 的坑

### 2.1 `num_ctx` 預設 2048,無視模型上限

**症狀**:模型答得出東西,但明顯沒讀到給它的文件;有時說「文件未提及」而文件明明有寫。

**原因**:Ollama 對每個請求套用 `num_ctx` 預設值 **2048**,不管模型宣稱支援多長。
`qwen2.5:7b` 宣稱 32768,實際只吃 2048。而一次檢索回傳 4~9k tokens,
**七成以上的文件在進模型之前就被丟掉,而且不會報錯**。

**怎麼發現的**:在提示最前面埋一個標記,再問模型那個標記是什麼。

```
num_ctx=2048    吃進 1026 tokens   標記找不到
num_ctx=8192    吃進 4623 tokens   標記找得到
num_ctx=32768   吃進 4623 tokens   標記找得到,只慢約 2 秒
```

**解法**:`providers/ollama_provider.py` 設定 `options.num_ctx = 16384`。

**影響**:這個 bug 讓先前所有 benchmark 數據作廢 —— 當時得到的「檢索是瓶頸」結論
其實無法成立,因為文件是被檢索到之後才丟掉的。

### 2.2 OpenAI 相容層無法設定 `num_ctx`

**症狀**:用 `openai` 套件、透過 `extra_body={"options": {"num_ctx": N}}` 傳,
`prompt_tokens` 仍然停在 2050。

**原因**:`/v1/chat/completions` 不吃 Ollama 專屬的 `options`。

**解法**:改用原生 `/api/chat`,它同樣支援 `tools`。這就是 `ollama_provider.py`
不使用 `openai` 套件的原因 —— 不是偏好,是相容層做不到。

### 2.3 原生端點拒絕 OpenAI 格式的對話紀錄(HTTP 400)

**症狀**:第一次呼叫成功、附上工具結果之後第二次就 400 Bad Request。

**原因**:兩種格式的差異:

| | OpenAI 相容層 | Ollama 原生 |
|---|---|---|
| `tool_calls` 的 `type` 欄位 | 有 | 不能有 |
| `arguments` | JSON **字串** | **物件** |
| 工具結果對應方式 | `tool_call_id` | `tool_name` |

**解法**:翻譯寫在 `OllamaProvider._to_native()`。
**刻意不寫在 `agent.py`** —— `base.py` 定義的介面就是 OpenAI 風格,
讓 agent 去遷就某一家後端,抽象層就失去意義了。

---

## 三、Agent 迴圈的坑

### 3.1 工具呼叫外洩成純文字(38% 的執行受影響)

**症狀**:回答裡出現這種東西,而且程式不當機:

```
 Closet
{"name": "search_documents", "arguments": {"query": "資工大三 避修"}}
```

前面那個無意義的 token(觀察到 `Escorts`、`portun`、`lett`、`ilor`、`Closet`)
是模型跑歪的產物。

**原因**:模型沒走 API 的 `tool_calls` 欄位,把呼叫請求當成一般文字輸出。
Ollama 不解析,`agent.py` 看到空的 `tool_calls` 就判定「不需要查文件」,直接作答。

**規模**:31 題 × 3 次 = 93 次執行中,**35 次(38%)完全沒有呼叫工具**,
其中 29 次是這種外洩。全程零錯誤訊息。

**診斷訊號**:靜默失敗平均 **0.8 秒**,正常查詢平均 **10.0 秒**。答得異常快就是沒查。

**解法**:`agent.py` 的 `_recover_leaked_tool_calls()` 把外洩的呼叫解析回來執行 ——
模型其實判斷對了,只是寫錯欄位,沒必要浪費。JSON 被截斷時(觀察過
`{"query": "退學 學分"}大大小` 這種)有 regex 退路。

**效果**:靜默失敗率 38% → 12%,殘骸外洩 29 次 → 3 次。

### 3.2 完全不查就作答

**症狀**:模型不呼叫工具,直接給出看似專業的答案,還會編出不存在的出處
(實測它引用過《學生手冊》裡沒有的「第11章 第1節」)。

**解法**:保底檢索。模型作答但 `retrieved_context` 是空的時候,
強制用原始問題查一次,把結果餵回去要求重答。
**只在完全沒查時才介入**,模型自己決定查什麼仍是主要路徑,agent 的規劃行為得以保留。

### 3.3 空白回答

**症狀**:`=== 最終回答 ===` 底下什麼都沒有。

**原因**:模型回傳空 `content` 且無工具呼叫時,`verify_answer()` 原樣回傳空字串。

**為什麼危險**:空白會讓使用者以為是網路問題,或更糟 —— 以為「查不到就是沒這條規定」。

**解法**:回傳明確的失敗訊息。**明講失敗比一片空白安全。**

---

## 四、檢索的坑

### 4.1 回傳整份文件 + 密度排序,系統性偏袒短文件

**症狀**:問「大三必修」,回傳的是修業規則和更換導師申請表,科目表進不了前三名。

**原因**:原本回傳「整份文件」並用「命中數 ÷ 文件長度」排序。
1KB 的修業規則命中兩次,密度就贏過 20KB 科目表命中十次。

**解法**:切成約 600 字的塊(沿段落邊界、保留 120 字重疊),讓長度可比。

### 4.2 只看關鍵字有無、`/sqrt(長度)` 過度獎勵短塊

**症狀**:切塊之後,課程地圖 PDF 被抽成的關鍵字湯(「大一 大二 大三 大四 必修
線性代數 機率⋯」)排第一,真正列出大三課程的那一塊排第 14。

**解法**:改用 **BM25**(k1=1.5, b=0.75)。詞頻會飽和但有效,長度是相對於語料平均
而非絕對值。

**教訓**:這個問題是**把分數印出來**才找到的。前面猜了三次都猜錯方向。

### 4.3 2-gram 產生洗版詞

**症狀**:查「抵免 轉學生」,第一名是「學生生活助學金作業要點」。

**原因**:2-gram 把「轉學生」拆出「學生」,而幾乎每份文件都有「學生」,
把真正關鍵的「抵免」洗掉了。

**解法**:IDF 加權。出現在越少塊裡的詞權重越高,罕見詞才是訊號。

### 4.4 章節被合併回去,關鍵字被稀釋

**症狀**:科目表裡「大一到大四」的必修整理擠在同一塊,查「大三」被其他年級稀釋。

**解法**:兩層 ——
`build_docs.py` 讓每個年級自成一段;`tools.py` 的 `_split()` 把 `【...】` 開頭的
章節標題當成**硬切點**,不與前一段合併(否則 150 字的短章節又會被黏回去)。

### 4.5 塊必須自我描述

**症狀**:【大三必修科目】那一塊只命中「大三」「必修」,輸給檔名有「資工系」的課程地圖。

**原因**:塊是被**單獨取出來**餵給模型的,它看不到檔名。

**解法**:章節標題與每一行都寫進系名與屆別
(`資工系大三上學期(三上)必修:...`)。這也讓模型讀到片段時知道自己在看哪一屆。

### 4.6 PDF 圖表抽成文字是關鍵字湯

課程地圖是圖，`pypdf` 抽出來變成沒有結構的詞彙堆,命中面積大但沒有實質內容,
會擠掉真正有答案的文件。目前靠 BM25 的長度正規化壓制,**沒有根本解決**。

---

## 五、資料與環境的坑

### 5.1 知識庫過期且有損(這是最嚴重的一個)

原本的 `docs/` 是手工摘要,出處只有檔案裡打的一行「來源:xxx.ncnu.edu.tw」。
後來對照官方 PDF 發現:

- 學士班科目表停在 **110 學年度**,而現行是 **115**
- 摘要時**整欄「開課年級」被丟掉** —— 導致「大三必修有哪些」在架構上就無解,
  再好的檢索也救不回來
- 共同課程/通識學分在 113 學年度起從 15/16 改成 **16/15**,舊摘要的數字對現在
  每一屆在學生都是錯的

**解法**:`docs/` 改為由 `docs_source/` 的官方 PDF 經 `scripts/build_docs.py` 自動產生,
`MANIFEST.tsv` 記錄每份的網址、下載時間、SHA-256。

**教訓**:**來源可查證優先於資料量。** 查不到官方出處的文件寧可刪掉,不要加警語留著 ——
警語不是出處,而混在裡面的未查證內容正是錯誤的來源。

### 5.2 同一份規定會依入學屆別分版本

科目表(111~115 各一份)、碩士班優秀學生獎勵辦法(112以前 / 113起)、
學士班新生入學獎勵辦法(114以前 / 115起)都是分屆發布的,而且**內容確實不同**。

只留最新版會答錯所有非應屆的學生。目前做法是各屆都收,檔名與內文標註適用屆別。

### 5.3 Windows / Git Bash 弄壞中文檔名

**症狀**:下載的檔案變成「學術倫理**教灶**課程實施要點.pdf」(育 U+80B2 → 灶 U+7076)。

**原因**:Git Bash 的 heredoc 與 argv 傳中文會產生 surrogate,寫檔時損壞。

**解法**:所有下載與轉檔都走 Python(`scripts/fetch.py`、`sources_*.py`),
來源清單寫在 `.py` 檔裡而不是用 shell 傳參。

### 5.4 主控台 cp950 印不出某些字元,整支腳本會中斷

**症狀**:轉檔跑到一半停掉,`UnicodeEncodeError: 'cp950' codec can't encode
character '立'`。

**原因**:某份官方文件標題含 CJK 相容字 U+F9F7;`✓`、`✗` 也印不出來。
**只是印出檔名就讓整個轉檔中斷。**

**解法**:每支腳本開頭 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`,
並對產生的檔名做 NFC 正規化(否則同一個字有兩種碼位,檔名比對會對不上)。

### 5.5 主控台編碼會讓「整個請求當掉」,不只是腳本

**這一個特別重要,因為它打在主程式上,而且真人使用時一定會踩到。**

**症狀**:`agent.py` 跑一半直接 Traceback,同一個問題有時好有時壞。

```
File "agent.py", line 153, in run_task
    print(f"[step {step}] 呼叫工具 {call['name']}({call['arguments']})")
UnicodeEncodeError: 'cp950' codec can't encode character '证'
```

**原因**:`证` 是「**证**」,簡體的「證」。agent 會把模型產生的查詢字串印進 log,
而模型**很常吐簡體**(這正是整條 pipeline 需要 OpenCC 的原因)。
只要模型送出「病假证明」這種查詢,**光是把它印出來就會讓整個請求死掉**。

實測同一題跑四次,**有一次崩潰**。

**解法**:`agent.py` 開頭把 stdout 與 stderr 都轉成 UTF-8。修正後連跑六次全部正常。

**教訓**:這類編碼問題不是只會影響輔助腳本。任何會把「模型產生的內容」寫進 log 的
地方都是地雷,而模型產生什麼你控制不了。

### 5.6 `| head` 會用 SIGPIPE 殺掉腳本

**症狀**:`python scripts/build_docs.py | head -1` 只產生 12 個檔案就結束,
而且看起來沒出錯。

**原因**:`head` 讀完一行就關閉管線,Python 寫入時收到 SIGPIPE。

**解法**:要看輸出就導到檔案再看,不要直接接 `head`。

### 5.7 量測工具的設定必須與被測系統一致

**症狀**:benchmark 回報「後處理後仍含簡體 13%」,看起來是 OpenCC 有漏洞。

**真相**:偵測器用 `s2t`,而 `agent.py` 的後處理用 `s2tw`,兩者轉換表不同 ——
把「群→羣」這種異體字正規化、以及「核**准**」的准(繁體本來就有這個字)
都算成簡體。改用 `s2tw` 重算,實際是 **0%**。

**教訓**:量測工具跟被量測系統設定不一致,就會**製造出不存在的問題**。

### 5.8 官方 PDF 的版面不只一種

同一種文件不同年度的排版會變:

- 111、112 學年度的科目表**沒有 `(R)` 修別標記**,113 以後才有
- 課名短時「代號 課名」同一行,課名長時代號自成一行、英文名跨兩三行

**解法**:`build_docs.py` 的正則兩種都吃,並用**公布的總學分數驗證**
(五屆都必須剛好是 49 學分)—— 有驗證才知道解析器有沒有漏抓。

---

## 六、向量索引

`index/` 是 `docs/` 每一塊的向量,由 `scripts/build_index.py` 用 **bge-m3** 產生
(988 x 1024,3.9MB,約 3 分鐘)。已經隨 repo 附上,一般不需要重建。

**為什麼要有它**:BM25 比對字面。學生問「診斷證明」,規則裡寫的是「校醫或醫院證明」,
沒有任何一個字重疊 —— 這不是調參數能解決的,是關鍵字檢索的結構性死角。
bge-m3 對這兩者的餘弦相似度是 0.787,對不相關的「宿舍冷氣費」是 0.451。

**融合方式**:Reciprocal Rank Fusion,不是加權相加。BM25 分數與餘弦相似度的尺度
完全不同,要加權得先正規化而且很難調;RRF 只看名次,而且其中一路失準時比較穩健。

**什麼時候要重建**:改動 `docs/` 之後。索引過期**不會報錯**,只會讓向量對到錯的塊,
所以 `tools.py` 會比對指紋,不一致就退回純 BM25 並印出提示。

**換 embedding 模型的話 `index/` 必須整個重建** —— 不同模型的向量空間不能混用。

---

## 七、快速自我檢查

接手之後想確認環境正常,依序跑:

```bash
python scripts/check_model.py qwen2.5:7b
```

```bash
python -c "from tools import search_documents; print(search_documents('大三 必修')[:300])"
```

```bash
python agent.py --task "資工系大三的必修有哪些?"
```

第三步應該看到 `[step 0] 呼叫工具 search_documents(...)`。
**如果直接跳到「草擬回答完成」,代表模型沒有查文件** —— 回頭看第一節。
