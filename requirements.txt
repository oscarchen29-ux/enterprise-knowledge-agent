# 繁簡後處理。模型(尤其中國訓練的開源模型)會吐簡體字,prompt 明講也未必有效。
opencc-python-reimplemented>=0.1.7

# 把 docs_source/ 的官方 PDF 轉成 docs/ 的文字知識庫(scripts/build_docs.py)。
pypdf>=4.0.0

# 向量索引的儲存與相似度計算(scripts/build_index.py、tools.py 的混合檢索)。
numpy>=1.24

# 選用:bge-m3 dense+sparse 檢索(scripts/build_m3_index.py、tools.py)。
# 需要 PyTorch,體積大(CPU 版約 2 GB 以上,另需下載模型約 2.2 GB),所以不列為必要。
# 沒裝時 tools.py 自動退回 BM25 + 向量 RRF。CPU 版安裝:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install FlagEmbedding
# Jetson(ARM)不能直接 pip install torch,要用 NVIDIA 為 JetPack 提供的版本。

# 網頁介面(web/app.py)。只有要跑網頁時才需要,CLI 與 benchmark 不依賴它。
flask>=3.0

# 註:不需要 openai 套件。Ollama 的 OpenAI 相容層無法設定 num_ctx
#（extra_body 傳 options 無效),而預設值 2048 會把檢索到的文件截掉七成,
# 因此 providers/ollama_provider.py 改用原生 /api/chat,只靠標準函式庫。
# 詳見 TROUBLESHOOTING.md 第 2.2 節。
