# Benchmark 執行結果 —— qwen2.5:7b

執行時間:2026-08-31 00:01
題庫:v3.0(32 題)× 3 次 = **96 次執行**
原始資料:`results_raw_qwen2.5-7b_20260830_233537.jsonl`
人工評分表:`scoring_sheet_qwen2.5-7b_20260830_233537.csv`

> **本頁只有自動量測的指標。答案正確率與幻覺率需人工評分**,評分標準見 `benchmark/README.md`。

本頁數字由 `python benchmark/recompute_metrics.py <jsonl>` 從原始資料重算,
不是手抄的。修正內容見文末「已知的量測問題」。

## 工具呼叫

| 指標 | 數值 |
|---|---|
| 有查文件 | 79% |
| 向使用者追問(未作答) | 22% |
| **靜默失敗(沒查也沒問)** | **3%** |
| 程式崩潰 | 0 次 |
| 達最大步驟數未完成 | 0 次 |
| 後處理前含簡體字 | 27% |
| 後處理後含簡體字 | 0%(應為 0%) |
| 延遲 | 平均 16.2s / 中位 13.0s / 最長 356s |

「追問」是模型判斷條件不足、呼叫 `ask_clarification` 把問題交還給使用者,
是設計中的正常行為,不是失效。三者相加為 104%,因為同一次執行可以先查了文件
再追問。

## 檢索

分母是「可答、而且真的發動了檢索」的執行。追問與靜默失敗的執行沒有檢索行為,
放進分母量到的會是「模型有沒有去查」與「查得準不準」的混合。

| 指標 | 數值 |
|---|---|
| 全命中 (n=60) | **70%** |
| 部分命中 | **90%** |
| 參考:分母含未檢索的執行 (n=78) | 54% / 69% |

## 分類拆解

| 類別 | 性質 | n | 有查文件 | 追問 | 檢索全命中 | 部分命中 | 平均秒 |
|---|---|---|---|---|---|---|---|
| `multi_hop` | 推理 | 30 | 70% | 37% | 76% | 95% | 11.2 |
| `unanswerable` | 推理 | 18 | 89% | 6% | 25% | 25% | 31.2 |
| `conditional` | 推理 | 9 | 100% | 0% | 100% | 100% | 13.8 |
| `cross_doc` | 抽取 | 18 | 72% | 33% | 23% | 85% | 12.8 |
| `procedural` | 抽取 | 9 | 67% | 22% | 100% | 100% | 14.2 |
| `single_doc` | 抽取 | 12 | 92% | 8% | 73% | 73% | 14.5 |

`unanswerable` 的檢索欄位沒有意義 —— 這類題目本來就不該有正確文件,
命中率低是預期結果。

**目前最大的單一弱點是 `cross_doc`**:部分命中 85%、全命中只有 23%,
意思是跨文件題幾乎每次都撈到需要的兩份裡的一份、另一份撈不到。
這是 top-k 或 RRF 融合的問題,不是模型能力問題。

## 需要追查的題目

**真的檢索了、卻一份正確文件都沒撈到**:

- `A02` 3/3 次 — expected: `資工系碩士班修業規則_1141120.txt`
- `B06` 2/3 次 — expected: `國立暨南國際大學學則.txt`、`學士班學生畢業外語能力基本要求實施要點_1150610修正.txt`
- `F08` 1/3 次 — expected: `學務處學生手冊_學習資源篇_p18-29.txt`、`學雜費收費標準.txt`

**靜默失敗**(沒查文件也沒追問,直接憑模型記憶作答):

- `C01` 2/3 次
- `D05` 1/3 次

兩題的 stdout 都是「模型回傳空白內容且未呼叫工具」,是同一條路徑。

**每次執行都以追問收尾、從未給出答案**:

- `B01` 3/3 次 —— 題目已寫明「轉系生、三年級」
- `F04` 3/3 次 —— 題目已寫明「轉學生、大二」

這兩題的條件在題目裡就給足了,模型仍然追問,屬於追問過度觸發。
人工評分時這六次執行會直接記為失敗。

## 已知的量測問題

這批原始資料產生時,執行器有兩個量測錯誤,兩個都已在 `run_benchmark.py` 修好,
本頁數字是用 `recompute_metrics.py` 從原始 jsonl 重算後的結果:

1. **檔名比對**:切塊檢索後 `returned_docs` 變成「檔名.txt 第N段」,而
   `expected_docs` 是純檔名,直接比會全部落空 —— 曾讓 96 次執行的檢索命中率
   全部顯示成 0%。
2. **追問被誤計成靜默失敗**:`no_tool_call` 原本定義為「`search_documents`
   一次都沒被呼叫」,而執行器只側錄了 `search_documents`,沒側錄
   `ask_clarification`。結果模型正常追問的執行被算成「沒查就答」,
   量出 **21%** 的靜默失敗率;20 筆裡有 17 筆其實是追問,真實值是 **3%**。

這是本專案第四次遇到「量測工具本身有問題」—— 另外三次記錄在
`TROUBLESHOOTING.md` 5.7 與 `benchmark/ablation_prompt.py`。
四次都是數字看起來合理、實際上量錯了東西。**在這個專案裡,一個看起來合理的
benchmark 數字,是量測錯誤的機率高於是真實結果的機率。**
